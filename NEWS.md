# preflightnma 0.0.1

* `nma_preflight_report()` generates a comprehensive, styled Quarto HTML report containing all preflight diagnostic outputs (sparsity, connectivity, transitivity, and covariate modifier plots).
